SaveGifPanelScript has been replaced with SaveGifUIScript which is at a different level of hierarchy to avoid subscription issues. There are demo prefabs showcasing this change.

SaveGifPanelScript will continue to work. Migration is only necessary to avoid having the Gif delegates called multiple times.
